# General
general {
    hide_cursor = true
}

# Authentication
auth {
    fingerprint:enabled = true
}

# Background
background {
    monitor =
    color = rgb(24272e)
}

# Widgets are generated per monitor so their apparent size follows output scale
source = $XDG_RUNTIME_DIR/hyprlock-widgets.conf
