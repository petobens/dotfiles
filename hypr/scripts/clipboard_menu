#!/usr/bin/env bash
set -euo pipefail

# Cliphist entries are "id<TAB>preview"; decode restores the original bytes
selection=$(cliphist list | rofi -dmenu -display-columns 2 -p clipboard) || exit 0
[[ -n $selection ]] || exit 0
printf '%s' "$selection" | cliphist decode | wl-copy
