#!/usr/bin/env bash
set -euo pipefail

# The menu contains entry names, not secrets
entry=$(gopass ls --flat | rofi -dmenu -p 'password')
[[ -n $entry ]] || exit 0
gopass show -o "$entry" | wl-copy --paste-once
notify-send -t 2000 "Password copied" "$entry"
